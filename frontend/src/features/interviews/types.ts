// Matches backend/app/features/interviews/schemas.py::InterviewsPlaceholder.
// Replace once the real analyze request/response contract is defined
// (shaped like app/models.py::ExitAnalysis).
export interface InterviewsPlaceholder {
  message: string
}
